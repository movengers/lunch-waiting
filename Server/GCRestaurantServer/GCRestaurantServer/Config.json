{
	NOTE: "Copy this file to the executable folder on the server.",
	config: 
	{
		naver_api:
		{
			client_id: "naver client id",
			client_secret: "key"
		},
		mysql:
		{
			host: "localhost",
			database: "db_name",
			id: "user_id",
			password:"password"
		}
	}
}